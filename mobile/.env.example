# Production: HTTPS API that matches server PUBLIC_URL / CORS (e.g. https://api.estrellx.shop)
# EAS: set EXPO_PUBLIC_API_URL in Project secrets or build profile env.
# Dev: leave unset to use LAN IP from Expo, or set http://YOUR_PC_LAN_IP:8080
EXPO_PUBLIC_API_URL=
